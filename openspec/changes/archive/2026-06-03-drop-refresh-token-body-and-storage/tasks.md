## 1. Backend — drop `refresh_token` from response bodies

- [x] 1.1 `apps/api/src/contexts/identity/adapters/inbound/http/schemas.py`: remove `refresh_token` from `TokenResponse` (the model returned by login, refresh, and confirm-signup auto-login). Update `_EXAMPLE_*` constants to drop the example field. Keep `SwitchTokenResponse` aligned.
- [x] 1.2 `apps/api/src/contexts/identity/adapters/inbound/http/router.py`: the `_set_refresh_cookie(response, identity.refresh_token)` calls stay; the `refresh_token=identity.refresh_token,` kwargs in the response constructor are removed. Audit all four mint sites (login, confirm-signup auto-login, refresh, switch).
- [x] 1.3 `apps/api/src/contexts/tenants/adapters/inbound/http/schemas.py`: remove `refresh_token` from `SwitchTokenResponse`. The request schema's `refresh_token: str | None` field stays for one transition cycle (server prefers cookie). **Scope extension:** also drop from `AcceptInvitationTokenBundle`; accept-invitation rotates the session on first-membership and needs to ship the cookie too — without this, dropping the SPA persistence would brick the post-accept session.
- [x] 1.4 `apps/api/src/contexts/tenants/adapters/inbound/http/router.py`: the switch endpoint's response no longer carries `refresh_token`. The use case prefers the cookie via the existing dependency wiring (`Request.cookies.get("nica_erp_rt")`). **Scope extension:** also wire `nica_erp_rt: Cookie` into `accept_invitation_by_body`, drop `refresh_token` from the tokens bundle, and set the rotated cookie when a session-rotation happens.
- [x] 1.5 OpenAPI snapshot: regenerate the schema if any committed `openapi.json` / `schema.d.ts` lives in the repo (`apps/web/src/api/schema.d.ts` is generated by `pnpm gen:api`).

## 2. SPA — drop sessionStorage persistence

- [x] 2.1 `apps/web/src/api/tokenStore.ts`: delete `STORAGE_KEY`, `readPersistedRefresh`, `writePersistedRefresh`, `clearPersistedRefresh`, the `refreshToken` module variable, the `getRefreshToken` export, and all `sessionStorage` writes from `setTokens` / `clear`. Keep `accessToken` and `idToken` in-memory.
- [x] 2.2 Update the `Tokens` interface to `{ access: string; id: string }` and the `setTokens` signature accordingly.
- [x] 2.3 Remove the comment block that documents the sessionStorage trade-off — it's no longer accurate.

## 3. SPA — interceptor uses cookie-only refresh

- [x] 3.1 `apps/web/src/api/interceptor.ts`: in `tryRefresh`, drop the `getRefreshToken()` read and the `body: JSON.stringify({ refresh_token: refreshToken })`. POST with `body: "{}"` (or omit and let openapi-fetch handle it) and `credentials: "include"`. Drop the early-return at line 84.
- [x] 3.2 In the same file: drop the `refresh: body.refresh_token` field from the `setTokens(...)` call after a successful refresh. The 401 propagation path stays unchanged.
- [x] 3.3 Remove the line 145 early-return `if (getRefreshToken() === null) return false;` — `bootRefresh` now always attempts one cookie refresh.

## 4. SPA — boot, switch, and logout wiring

- [x] 4.1 `apps/web/src/app.tsx`: change `useState(() => getRefreshToken() === null)` to `useState(false)` so `bootRefresh` always runs once on mount. Drop the `getRefreshToken` import.
- [x] 4.2 `apps/web/src/components/app-sidebar/tenant-switcher.tsx`: stop reading `getRefreshToken()`; call `useSwitchTenantMutation` with an empty input. Drop the `getRefreshToken` import.
- [x] 4.3 `apps/web/src/routes/tenants/new.tsx`: the post-create switch (line ~382) no longer needs `getRefreshToken()`; pass `{}` as the switch input. Drop the import. **Also** `apps/web/src/routes/tenants/index.tsx` (tenant picker) and `apps/web/src/routes/invitations/accept.tsx` + `apps/web/src/features/tenants/api/endpoints.ts:acceptInvitation` (no longer take refresh_token arg).
- [x] 4.4 `apps/web/src/features/tenants/api/hooks.ts`: `useSwitchTenantMutation`'s input type drops `refresh_token`. The body sent to `/v1/tenants/{id}/switch` becomes `{}`. The `setTokens(...)` call after the rotation drops the `refresh:` field.
- [x] 4.5 `apps/web/src/features/auth/api/hooks.ts`: the `setTokens(...)` calls after login / confirm-signup drop the `refresh:` field. Login mutation no longer expects `refresh_token` in the response.

## 5. Tests

- [x] 5.1 Backend integration tests under `apps/api/tests/integration/api/auth`: replace `assert resp.json()["refresh_token"]` with `assert resp.cookies.get("nica_erp_rt") is not None` and `assert "refresh_token" not in resp.json()`. Apply to login, refresh, confirm-signup auto-login, switch. **Also** updated `test_invitations_router.py` (accept rotation) + `test_tenants_router.py` (switch).
- [x] 5.2 Backend e2e `test_login_sets_refresh_cookie` and `test_full_local_auth_loop`: assert the body shape excludes `refresh_token` and that the cookie carries it. **Also** updated `test_logout_revokes_refresh_token_and_blocks_subsequent_refresh`, `test_refresh_token_rejected_on_bearer_auth_path`, `test_rls_tenant_isolation` (both functions), and `test_tenant_lifecycle.test_owner_walks_create_switch_invite_lifecycle` to extract the cookie from the login response and pass it explicitly on subsequent calls (httpx doesn't auto-store `Secure` cookies over plain http://).
- [x] 5.3 Backend integration: `test_post_switch_tenant_returns_token_bundle` now exercises `cookies={"nica_erp_rt": ...}` with empty body. The dedicated cookie-only refresh test was deferred — every test that hits `/v1/auth/refresh` (including `test_refresh_token_rejected_on_bearer_auth_path` and the lifecycle suite) already does this end-to-end. **Scope addendum:** broadened cookie `Path=/v1` (was `/v1/auth`) so it reaches `/v1/tenants/.../switch` and `/v1/invitations/accept` — narrow enough to skip non-versioned routes, wide enough to cover every endpoint that rotates a refresh token. Spec, source, and tests updated coherently.
- [x] 5.4 SPA `apps/web/tests/unit/api/tokenStore.test.ts`: drop the "persists refresh to sessionStorage" cases; add a "setTokens / clear never write to sessionStorage" assertion.
- [x] 5.5 SPA `apps/web/tests/unit/api/interceptor.test.ts` + `tests/integration/features/tenants/api/hooks.spec.tsx`: update `tryRefresh` mocks to assert the request has `credentials: "include"` and an empty body; remove `refresh_token` from the mocked response.

## 6. Validation

- [x] 6.1 `openspec validate drop-refresh-token-body-and-storage --strict` exits 0.
- [x] 6.2 Manual smoke: login → switch tenant → reload → switch tenant. Confirmed in vivo: `sessionStorage.getItem('nica-erp:refresh-token')` is `null` throughout, the cookie persists across reload and `bootRefresh` recovers the session via the cookie alone (round-trip 200, no body field needed). **Bug caught:** `RefreshRequest.refresh_token` was required, so the bootRefresh POST with empty body returned 422 — flipped the field to `str | None`, regenerated schema.d.ts.
- [x] 6.3 Re-run the F-011 audit step: after a fresh login, `Object.keys(sessionStorage)` is `["tsr-scroll-restoration-v1_3"]` (or `last-app-route/picker-confirmed` after a switch) — no `nica-erp:refresh-token`. `document.cookie` from JS is empty (`HttpOnly` flag working). Cookie is delivered in the `Set-Cookie` response header only; JS never sees it.
