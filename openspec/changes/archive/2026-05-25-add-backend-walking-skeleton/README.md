# add-backend-walking-skeleton

Sprint 00 — Walking skeleton (backend + infrastructure): monorepo bootstrap, FastAPI on :8000, Postgres+LocalStack+Mailpit via Docker Compose, Alembic migration 0001 with shared_kernel tables, shared_kernel domain/application/adapters, /healthz endpoint, import-linter contract, CI workflows, pre-commit hooks, Makefile targets.
